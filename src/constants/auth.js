/**
 * 인증 관련 상수 (COOKIE_GUIDE §4)
 * work_fo의 CrossDomainTokenManager 쿠키 이름과 동일해야 함
 */

/** 쿠키에 저장되는 Access Token 키 (work_fo: shared_access_token) */
export const COOKIE_ACCESS_TOKEN = 'shared_access_token';
